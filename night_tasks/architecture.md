## Objective: Ensure scalable project structure
**Investigation:** Map the dependency graph and folder structure. Identify circular dependencies, tight coupling between layers (UI, logic, data), and large monolithic files.
**Action:** Reorganize the code into clear, logical layers or feature-based modules. Decouple the business logic from the UI components. Ensure that changing one part of the app does not unexpectedly break another.
